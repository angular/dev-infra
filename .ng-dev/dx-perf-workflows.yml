workflows:
  rerun:
    name: Rerun a test
    workflow:
      - bazel test //ng-dev/utils/test;

  build-everything:
    name: Build Everything
    prepare:
      - bazel clean;
    workflow:
      - bazel build //...;
