load("//tools:defaults.bzl", "esbuild_checked_in")

esbuild_checked_in(
    name = "post",
    entry_point = "//github-actions/breaking-changes-label/lib:post.ts",
    deps = [
        "//github-actions/breaking-changes-label/lib",
    ],
)

esbuild_checked_in(
    name = "main",
    entry_point = "//github-actions/breaking-changes-label/lib:main.ts",
    deps = [
        "//github-actions/breaking-changes-label/lib",
    ],
)
