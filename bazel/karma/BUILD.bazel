package(default_visibility = ["//visibility:public"])

exports_files(["karma-local-config.js"])

# Make source files available for distribution via pkg_npm
filegroup(
    name = "files",
    srcs = glob(["*"]),
)
