load("//tools:defaults.bzl", "jasmine_node_test", "ts_library")

ts_library(
    name = "unit_test_lib",
    testonly = True,
    srcs = glob([
        "**/*.spec.ts",
    ]),
    deps = [
        "//bazel/markdown_to_html/src",
        "@npm//@bazel/runfiles",
        "@npm//@types/jasmine",
        "@npm//@types/node",
    ],
)

jasmine_node_test(
    name = "unit_tests",
    data = glob(["**/*.md"]),
    specs = [":unit_test_lib"],
)
