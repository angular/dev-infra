load("//tools:defaults_new.bzl", "ts_library")

ts_library(
    name = "test_lib",
    testonly = True,
    srcs = glob([
        "**/*.ts",
    ]),
    deps = [
        "//:node_modules/@types/jasmine",
        "//:node_modules/@types/node",
        "//ng-dev/utils",
        "//ng-dev/utils/testing",
    ],
)

#jasmine_node_test(
#    name = "test",
#    data = ["//:node_modules/@yarnpkg/lockfile"],
#    # Same reasoning as in "ng-dev/BUILD.bazel". This package cannot be bundled.
#    external = ["@yarnpkg/lockfile"],
#    specs = [":test_lib"],
#)
