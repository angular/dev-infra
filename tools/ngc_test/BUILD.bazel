load("//tools:defaults_new.bzl", "ng_module")

ng_module(
    name = "ngc_test",
    srcs = [
        "main.ts",
        "template.html",
    ],
    tsconfig = "tsconfig.json",
    deps = ["//:node_modules/@angular/core"],
)
